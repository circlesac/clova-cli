{
  "name": "@circlesac/clova",
  "type": "module",
  "description": "CLOVA Note CLI — transcripts, summaries, speakers, and audio from the terminal",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/circlesac/clova-cli"
  },
  "module": "src/index.ts",
  "bin": {
    "clova": "bin/clova"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "citty": "^0.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vitest/coverage-v8": "^4.0.18",
    "playwright": "^1.48.0",
    "vitest": "^4.0.18"
  },
  "peerDependencies": {
    "playwright": "^1.40.0",
    "typescript": "^5"
  },
  "peerDependenciesMeta": {
    "playwright": {
      "optional": true
    }
  },
  "files": [
    "bin",
    "skills"
  ],
  "pi": {
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "build": "bun build --compile --external playwright --outfile=dist/clova src/index.ts",
    "dev": "bun run src/index.ts",
    "test": "vitest run",
    "lint": "npx @circlesac/lint --all",
    "type-check": "npx tsc --noEmit",
    "pre-merge": "bun run lint && bun run type-check && bun run test && bun run build",
    "postinstall": "node bin/install.js"
  }
}
